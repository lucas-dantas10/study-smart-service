server:
  port: 8080
spring:
  application:
    name: study_smart_service
  datasource:
    url: jdbc:postgresql://localhost:5434/study_smart
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
